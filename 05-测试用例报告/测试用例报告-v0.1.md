# 减肥 PK 小程序 — 测试用例报告 v0.1

> **版本**：v0.1 | **日期**：2026-07-14 | **作者**：CC【测试】
> **状态**：用例框架已完成，预期值部分因文档冲突（O-8~O-11）暂标「⏳待确认」，O-2~O-11 由产品确认后当天锁定预期值
>
> **适用范围**：L1 自用版（Deadline 2026-08-01）
>
> **关联文档**：PRD-v0.1 | MVP功能清单-v0.1 | 接口约定-v0.1 | DB-schema-v0.1 | 部署方案-v0.1 | DR-001~DR-006 | 测试策略-v0.3

---

## 一、说明

### 1.1 用例优先级定义

| 优先级 | 含义 |
|--------|------|
| **P0** | 上线阻塞项，必须 0 遗留。核心链路/资金安全/数据准确/隐私合规 |
| **P1** | 高优先级，不可遗留。重要功能路径、边界场景 |
| **P2** | 中优先级，可遗留 ≤2 个且有规避方案。体验类、非核心功能 |
| **P3** | 低优先级，后续迭代修复。细节优化、Nice-to-have |

### 1.2 测试状态定义

| 状态 | 含义 |
|------|------|
| ⏳ 待执行 | 用例已设计，等待环境就绪或接口可用 |
| ✅ 通过 | 用例已执行，结果符合预期 |
| ❌ 失败 | 发现缺陷，已提 Bug |
| ⚠️ 待确认 | 预期值依赖产品确认（O-2~O-11） |
| 🚫 阻塞 | 外部依赖未就绪，无法执行 |

### 1.3 已锁定核心规则（DR-001~DR-006）

| 规则 | 结论 | 来源 |
|------|------|------|
| 积分权重 | 减重70% / 打卡15% / 饮食8% / 运动7% | DR-005 |
| K 值 | K=1（线性映射） | DR-005 |
| 积分精度 | 存储4位浮点，展示2位四舍五入 | DR-003 Q1 |
| 并列排名 | 不跳号（1,1,2） | DR-003 Q2 |
| 结束日打卡 | 当天23:59前有效 | DR-003 Q3 |
| 积分更新 | T+1，次日00:05 | DR-003 Q4 |
| 初始体重 | 不可修改 | DR-003 Q5 |
| 退出规则 | 标记"已退出"，历史保留，房主不可直接退出 | DR-003 Q6 |
| 积分分母 | 用户加入后实际经过天数 | DR-006 Q8 |
| 拍照防作弊 | 强制相机+Canvas水印（时间+昵称），禁相册 | DR 已拍板 |
| 体重字段 | 字段级加密存储 | 后端技术选型 |

---

## 二、用例总览

| 模块 | 用例数 | P0 | P1 | P2 | P3 |
|------|--------|----|----|----|----|
| M1 用户体系 | 7 | 4 | 3 | 0 | 0 |
| M2 发起PK | 9 | 4 | 5 | 0 | 0 |
| M3 加入PK | 8 | 3 | 4 | 1 | 0 |
| M4 每日打卡 | 13 | 6 | 5 | 2 | 0 |
| M5 积分计算 | 14 | 11 | 3 | 0 | 0 |
| M5 排行榜 | 5 | 2 | 2 | 1 | 0 |
| M6 结算+海报 | 9 | 6 | 2 | 1 | 0 |
| M7 合规 | 11 | 9 | 2 | 0 | 0 |
| 退出与解散 | 7 | 0 | 5 | 2 | 0 |
| 接口层测试 | 30+ | 15 | 15 | — | — |
| **合计** | **113+** | **60** | **46** | **7** | **0** |

---

## 三、模块详细用例

### 3.1 M1 用户体系

#### TC-AUTH-001｜首次微信授权登录

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 全新用户，未曾登录过 |
| **操作步骤** | 1. 打开小程序 2. 点击「授权登录」 3. 在微信授权弹窗点击「允许」 |
| **预期结果** | ① 获取有效 token ② 用户信息（昵称+头像）正确写入 ③ 跳转首页 |
| **关联接口** | `POST /api/auth/login` |
| **状态** | ⏳ 待执行 |

#### TC-AUTH-002｜已登录用户再次进入（token 有效）

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 用户已登录，token 未过期 |
| **操作步骤** | 1. 关闭小程序 2. 重新打开 |
| **预期结果** | ① 免登直接进入首页 ② 不再弹授权框 ③ 用户信息正常显示 |
| **状态** | ⏳ 待执行 |

#### TC-AUTH-003｜Token 过期后自动刷新

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | 用户已登录，手动使 token 过期（修改本地 storage 或等待过期） |
| **操作步骤** | 1. 使 token 过期 2. 发起任意需要认证的请求 |
| **预期结果** | ① 后端返回 1002 错误 ② 前端自动调用 wx.login 获取新 code ③ 静默刷新 token ④ 原请求重试成功，用户无感知 |
| **状态** | ⏳ 待执行（需接口联调后） |

#### TC-AUTH-004｜首次使用弹出敏感信息同意弹框

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 全新用户首次进入打卡/体重录入页面 |
| **操作步骤** | 1. 进入体重打卡页面 2. 查看是否弹出《敏感个人信息处理说明》 |
| **预期结果** | ① 弹出单独同意弹框 ② 弹框内容含「体重数据属于健康类敏感信息」说明 ③ 必须主动点击「同意」才能继续 |
| **状态** | ⏳ 待执行 |

#### TC-AUTH-005｜已同意后不再重复弹框

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | 用户已同意敏感信息说明 |
| **操作步骤** | 1. 再次进入打卡页面 |
| **预期结果** | 不再弹出同意框 |
| **状态** | ⏳ 待执行 |

#### TC-AUTH-006｜拒绝同意后不可进入体重录入

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 全新用户 |
| **操作步骤** | 1. 在同意弹框点击「拒绝/取消」 |
| **预期结果** | ① 无法进入打卡页面 ② 弹框提示「需要同意才能使用打卡功能」 |
| **状态** | ⏳ 待执行 |

#### TC-AUTH-007｜「我的」页面数据删除

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 用户有历史打卡数据 |
| **操作步骤** | 1. 进入「我的」页面 2. 找到「删除账号及数据」入口 3. 二次确认 4. 提交删除 |
| **预期结果** | ① 用户数据全部清除 ② token 失效 ③ DB 中该用户所有记录已删除（需 DB 只读权限验证）④ 再次登录视为全新用户 |
| **关联接口** | `DELETE /api/user/account` |
| **状态** | ⏳ 待执行 |

---

### 3.2 M2 发起 PK 房间

#### TC-PK-001｜创建房间正常路径

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 用户已登录 |
| **操作步骤** | 1. 填写房间名称「夏日减脂PK」 2. 设置周期 21 天 3. 今天开始 4. 提交创建 |
| **预期结果** | ① 房间创建成功，状态 ACTIVE ② 生成唯一邀请码 ③ 跳转房间详情页 ④ 创建者自动成为房主 |
| **关联接口** | `POST /api/room/create` |
| **状态** | ⏳ 待执行 |

#### TC-PK-002｜周期边界值：7天（最短）

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **输入** | 周期 = 7 天 |
| **预期结果** | 创建成功 |
| **状态** | ⏳ 待执行 |

#### TC-PK-003｜周期边界值：90天（最长）

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **输入** | 周期 = 90 天 |
| **预期结果** | 创建成功 |
| **状态** | ⏳ 待执行 |

#### TC-PK-004｜周期非法：6天（低于最短）

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **输入** | 周期 = 6 天 |
| **预期结果** | 返回错误 1001（参数校验失败），提示「最短 7 天」 |
| **状态** | ⏳ 待执行 |

#### TC-PK-005｜周期非法：91天（超过最长）

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **输入** | 周期 = 91 天 |
| **预期结果** | 返回错误 1001，提示「最长 90 天」 |
| **状态** | ⏳ 待执行 |

#### TC-PK-006｜初始体重录入 + 禁止相册

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **操作步骤** | 1. 创建房间时录入初始体重 2. 点击拍照按钮 |
| **预期结果** | ① 拍照组件仅开放相机（无相册入口）② Canvas 水印正确合成（时间+昵称）③ 不含地理位置 |
| **状态** | ⏳ 待执行 |

#### TC-PK-007｜初始体重边界值

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **测试数据** | 30.0（通过）/ 300.0（通过）/ 29.9（拒绝）/ 300.1（拒绝）/ 0（拒绝）/ 负数（拒绝）|
| **预期结果** | 边界内通过，边界外返回 1001 |
| **状态** | ⏳ 待执行 |

#### TC-PK-008｜邀请码/分享码生成

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **预期结果** | ① 生成 6 位邀请码 ② 可生成小程序码 ③ 可分享到微信 |
| **状态** | ⏳ 待执行 |

#### TC-PK-009｜房主解散房间

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **操作步骤** | 1. 房主点击「解散房间」 2. 二次确认 |
| **预期结果** | ① 房间状态变为 DISSOLVED ② 所有成员只读 30 天 ③ 不触发结算 ④ 无结算海报 |
| **关联接口** | `POST /api/room/dissolve` |
| **状态** | ⏳ 待执行 |

---

### 3.3 M3 加入 PK

#### TC-JOIN-001｜通过邀请码加入

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **操作步骤** | 1. 输入有效6位邀请码 2. 查看规则确认页 3. 录入初始体重+拍照 4. 确认加入 |
| **预期结果** | ① 加入成功 ② 初始体重正确存储（加密）③ 排行榜显示该用户 |
| **状态** | ⏳ 待执行 |

#### TC-JOIN-002｜通过小程序码/分享链接加入

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **操作步骤** | 1. 扫描/点击小程序码 2. 进入加入确认页 |
| **预期结果** | 同 TC-JOIN-001 |
| **状态** | ⏳ 待执行 |

#### TC-JOIN-003｜无效邀请码

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **输入** | 不存在的邀请码「XXXXXX」 |
| **预期结果** | 返回 2001（房间不存在），提示「邀请码无效」 |
| **状态** | ⏳ 待执行 |

#### TC-JOIN-004｜重复加入同一房间

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | 用户已在该 PK 房间中 |
| **预期结果** | 返回 2002（已在房间中），不重复计费 |
| **状态** | ⏳ 待执行 |

#### TC-JOIN-005｜PK 已结束后加入

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | 房间状态为 SETTLED |
| **预期结果** | 返回 2003（PK 已结束），拒绝加入 |
| **状态** | ⏳ 待执行 |

#### TC-JOIN-006｜加入确认页内容展示

| 字段 | 内容 |
|------|------|
| **优先级** | P2 |
| **预期结果** | 展示：PK名称、发起人、周期、当前参与人数、规则说明 |
| **状态** | ⏳ 待执行 |

#### TC-JOIN-007｜同一用户加入多个 PK 房间

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **预期结果** | 可同时参与多个房间，各房间积分独立计算 |
| **状态** | ⏳ 待执行 |

#### TC-JOIN-008｜迟加入者积分分母验证

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **场景** | PK 共 21 天，用户第 3 天加入，现在是第 10 天 |
| **预期结果** | 打卡积分分母 = 8（第3天到第10天已过8天），不是 21，不是 10 |
| **关联规则** | DR-006 Q8 |
| **状态** | ⏳ 待执行 |

---

### 3.4 M4 每日打卡

#### TC-CI-001｜正常打卡：全维度

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **操作步骤** | 1. 拍摄体重秤照片（Canvas 水印） 2. 输入体重 70.5kg 3. 输入饮食记录（文字≥10字+1张图片）4. 输入运动记录（文字+可选图片）5. 提交 |
| **预期结果** | ① 打卡成功 ② 当日打卡状态变为「已打卡」③ 积分实时更新（T+1 00:05 显示更新）|
| **状态** | ⏳ 待执行 |

#### TC-CI-002｜仅体重打卡（饮食运动不填）

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **操作步骤** | 仅填体重+拍照，饮食运动留空 |
| **预期结果** | 打卡成功，饮食/运动维度当日得分 = 0 |
| **状态** | ⏳ 待执行 |

#### TC-CI-003｜体重输入边界值

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **测试数据** | 30.0（✅）/ 300.0（✅）/ 30.05（✅）/ 29.9（❌）/ 300.1（❌）/ 0（❌）/ 负数（❌）/ 非数字「abc」（❌）|
| **预期结果** | 边界内通过，边界外返回 1001 |
| **状态** | ⏳ 待执行 |

#### TC-CI-004｜拍照水印验证

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **操作步骤** | 1. 点击拍照 2. 拍摄并确认 3. 查看水印 |
| **预期结果** | ① 无相册入口（`sourceType: ['camera']`）② 水印含当前时间（精确到分钟）③ 水印含用户昵称（脱敏）④ 水印不含地理位置 |
| **状态** | ⏳ 待执行 |

#### TC-CI-005｜PK 未开始时打卡

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | PK 状态为 PENDING（未到开始日期）|
| **预期结果** | 返回 3002，提示「PK 还未开始」 |
| **状态** | ⏳ 待执行 |

#### TC-CI-006｜PK 已结束后打卡

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | PK 状态为 SETTLED |
| **预期结果** | 返回 3002，提示「PK 已结束」 |
| **状态** | ⏳ 待执行 |

#### TC-CI-007｜同日重复打卡（⚠️ 预期值待确认）

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **场景** | 当日已打卡一次，再次打卡 |
| **预期结果** | ⚠️ **O-10 冲突未解决**：DR-006 = 允许覆盖取最新；PRD M3 = 不允许重新提交。**待菲菲确认后锁定。** |
| **状态** | ⚠️ 待确认 |

#### TC-CI-008｜PK 开始日 00:01 打卡

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **预期结果** | 打卡成功（PK 开始日第一分钟即有效） |
| **状态** | ⏳ 待执行 |

#### TC-CI-009｜PK 结束日 23:59 打卡

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **预期结果** | 打卡成功（DR-003 Q3：结束日 23:59 前有效）|
| **关联规则** | DR-003 Q3 |
| **状态** | ⏳ 待执行 |

#### TC-CI-010｜并发提交两次打卡

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **操作步骤** | 同时发起两次打卡请求（Charles 重放/并发工具）|
| **预期结果** | 只成功一次，另一次返回 3001。DB 无重复记录（唯一索引保障）|
| **状态** | ⏳ 待执行（接口联调后）|

#### TC-CI-011｜体重与初始体重偏差 >40% 拒绝

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **场景** | 初始 80kg，当日录入 47kg（偏差 41.25%）|
| **预期结果** | 拒绝提交，提示「体重数据异常，请确认」|
| **状态** | ⏳ 待执行 |

#### TC-CI-012｜相邻体重差 >5kg 需二次确认

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **场景** | 昨日 80kg，今日录入 74.5kg（差 5.5kg）|
| **预期结果** | 弹出「体重波动较大，确认录入？」确认框，确认后成功提交 |
| **状态** | ⏳ 待执行 |

#### TC-CI-013｜网络异常时打卡（本地暂存）

| 字段 | 内容 |
|------|------|
| **优先级** | P2 |
| **场景** | 断网状态下完成打卡填写，提交 |
| **预期结果** | 数据本地暂存，恢复网络后自动重传（如排期支持，否则降级提示）|
| **状态** | ⏳ 待执行 |

---

### 3.5 M5 积分计算（P0 必须独立复核）

> **积分公式（K=1 已锁定）**：
> ```
> 综合积分 = 减重得分 + 打卡得分 + 饮食得分 + 运动得分
>
> 减重得分  = min(减重%, 100) × 0.70
> 打卡得分  = (打卡天数 / 加入后实际经过天数) × 100 × 0.15
> 饮食得分  = (饮食打卡天数 / 加入后实际经过天数) × 100 × 0.08
> 运动得分  = (运动打卡天数 / 加入后实际经过天数) × 100 × 0.07
>
> 减重% = (初始体重 - 最新体重) / 初始体重 × 100
>         ⚠️ "最新体重"定义：O-8 冲突待确认（单次最新 vs 3日均值）
>
> 存储：4位浮点 | 展示：2位四舍五入
> ```

#### TC-SCORE-001｜标准减重场景（10%减重 + 全勤）

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **输入** | 初始 75kg → 最新 67.5kg，21天全勤，饮食运动100% |
| **手算预期** | 减重%=10%；减重得分=min(10,100)×0.7=**7.00**；打卡=100×0.15=**15.00**；饮食=100×0.08=**8.00**；运动=100×0.07=**7.00**；**总分=37.00** |
| **状态** | ⏳ 待执行 |

#### TC-SCORE-002｜5%减重 + 全勤

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **输入** | 初始 80kg → 最新 76kg，21天全勤，饮食运动100% |
| **手算预期** | 减重%=5%；减重得分=5×0.7=**3.50**；**总分=33.50** |
| **状态** | ⏳ 待执行 |

#### TC-SCORE-003｜增重场景（减重积分下限）

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **输入** | 初始 75kg → 最新 77kg，全勤 |
| **手算预期** | 减重%=-2.667%；⚠️ **O-2 待确认**：若下限0则减重得分=0，总分=**30.00**；若允许负分则减重得分=-1.87，总分=**28.13** |
| **状态** | ⚠️ 待确认 |

#### TC-SCORE-004｜体重持平 + 全勤

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **输入** | 体重无变化，全勤 |
| **手算预期** | 减重得分=0；**总分=0+15+8+7=30.00** |
| **状态** | ⏳ 待执行 |

#### TC-SCORE-005｜打卡率 50%

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **输入** | 10/20天打卡，饮食运动100%，体重持平 |
| **手算预期** | 打卡=50×0.15=**7.50**；饮食=100×0.08=**8.00**；运动=100×0.07=**7.00**；**总分=22.50** |
| **状态** | ⏳ 待执行 |

#### TC-SCORE-006｜饮食/运动部分完成

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **输入** | 全勤体重打卡，饮食80%，运动60%，体重持平 |
| **手算预期** | 饮食=80×0.08=**6.40**；运动=60×0.07=**4.20**；打卡=**15.00**；**总分=25.60** |
| **状态** | ⏳ 待执行 |

#### TC-SCORE-007｜全部 0 分场景

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **输入** | 0 打卡天数，0 饮食，0 运动，体重持平 |
| **预期结果** | **总分=0.00** |
| **状态** | ⏳ 待执行 |

#### TC-SCORE-008｜并列排名不跳号

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **输入** | 用户A=37.00分，用户B=37.00分，用户C=30.00分 |
| **预期结果** | A、B 并列第1名，C 为第2名（不跳号，即 1,1,2）|
| **关联规则** | DR-003 Q2 |
| **状态** | ⏳ 待执行 |

#### TC-SCORE-009｜积分精度

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **场景** | 某计算结果为 33.4567 |
| **预期结果** | DB 存储 33.4567（4位浮点）；页面展示 33.46（四舍五入2位）|
| **状态** | ⏳ 待执行 |

#### TC-SCORE-010｜微小减重精度

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **输入** | 初始 80kg → 最新 79.9kg（减重 0.125%）|
| **手算预期** | 减重得分=0.125×0.7=0.0875→展示0.09；全勤时总分≈30.09 |
| **状态** | ⏳ 待执行 |

#### TC-SCORE-011｜减重超100%的封顶处理

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **输入** | 理论上减重%>100（人为构造数据）|
| **预期结果** | min(...,100)封顶，减重得分最高=70 |
| **状态** | ⏳ 待执行 |

#### TC-SCORE-012｜迟加入者分母验证

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **场景** | PK共21天，第3天加入，现第10天 |
| **预期结果** | 打卡分母=8（第3天到第10天的8天），不是21也不是10 |
| **关联规则** | DR-006 Q8 |
| **状态** | ⏳ 待执行 |

#### TC-SCORE-013｜结算快照独立性

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **场景** | 结算触发后，对比结算数据与 T+1 日常更新数据 |
| **预期结果** | 结算快照从原始数据独立重算，与日常缓存一致；结算后数据不可变 |
| **状态** | ⏳ 待执行 |

#### TC-SCORE-014｜积分更新时机（T+1 00:05）

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **操作步骤** | 在 T 日 23:30 打卡，记录排行榜；等到 T+1 00:05 后刷新排行榜 |
| **预期结果** | T+1 00:05 之后积分和排名更新，00:05 之前保持 T-1 数据 |
| **状态** | ⏳ 待执行 |

---

### 3.6 M5 排行榜

#### TC-RANK-001｜排行榜展示字段

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **预期结果** | 展示：昵称（脱敏）+ 头像 + 积分 + 减重%（不含绝对体重）+ 排名，降序排列 |
| **状态** | ⏳ 待执行 |

#### TC-RANK-002｜绝对体重不出现在接口返回

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **验证方式** | Charles 抓包排行榜接口 `GET /api/leaderboard` |
| **预期结果** | Response JSON 中不含 `weight`、`currentWeight`、`initialWeight` 等绝对体重字段（他人数据）|
| **状态** | ⏳ 待执行 |

#### TC-RANK-003｜已退出用户展示

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **预期结果** | 已退出用户排在末位，标记「已退出」，不参与名次排序 |
| **状态** | ⏳ 待执行 |

#### TC-RANK-004｜最后更新时间显示

| 字段 | 内容 |
|------|------|
| **优先级** | P2 |
| **预期结果** | 排行榜页脚显示「最后更新：昨天 00:05」（T+1 对应时间）|
| **状态** | ⏳ 待执行 |

#### TC-RANK-005｜个人详情页

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **预期结果** | 点击自己头像查看：体重曲线图 + 打卡日历 + 各维度得分；他人只看减重% + 打卡率，不看体重曲线 |
| **状态** | ⏳ 待执行 |

---

### 3.7 M6 PK 结算 + 战报海报

#### TC-STL-001｜定时结算触发

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **场景** | PK 到期日 00:00 自动触发结算任务 |
| **验证方式** | 服务端日志确认定时任务执行 |
| **预期结果** | 00:00 触发，生成结算快照，状态变为 SETTLED |
| **状态** | ⏳ 待执行 |

#### TC-STL-002｜结算幂等性（重复触发）

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **操作步骤** | 手动多次触发结算接口 |
| **预期结果** | 结算结果不变，无重复结算快照 |
| **状态** | ⏳ 待执行 |

#### TC-STL-003｜结算详情页展示

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **预期结果** | 展示：最终排名 + 各成员成绩卡（昵称+积分+减重%）；本人可见自己绝对体重；他人绝对体重不展示 |
| **状态** | ⏳ 待执行 |

#### TC-STL-004｜绝对体重仅本人可见（接口层）

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **验证方式** | Charles 抓包结算接口；用 UserA token 调用，验证 Response 中 UserB 无绝对体重字段 |
| **预期结果** | 他人绝对体重不在 Response 中返回 |
| **状态** | ⏳ 待执行 |

#### TC-STL-005｜战报海报 Canvas 生成

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **预期结果** | 海报包含：用户名次 + 减重% + PK名称 + PK日期 + Canvas 渲染正确 |
| **状态** | ⏳ 待执行 |

#### TC-STL-006｜海报保存到相册 + 分享

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **操作步骤** | 1. 点击「保存海报」2. 点击「分享到微信」|
| **预期结果** | ① 保存到系统相册成功 ② 分享到微信好友/朋友圈成功 |
| **状态** | ⏳ 待执行 |

#### TC-STL-007｜结算后数据不可变

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **操作步骤** | 结算完成后，尝试再次触发结算 |
| **预期结果** | 结算数据不变，接口返回「PK 已结算」|
| **状态** | ⏳ 待执行 |

#### TC-STL-008｜未打卡用户结算

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **场景** | 某成员全程 0 打卡 |
| **预期结果** | 减重得分=0，打卡/饮食/运动得分=0，总分=0，正常参与排名（排末位）|
| **状态** | ⏳ 待执行 |

#### TC-STL-009｜最终体重取值验证

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **场景** | 用户最后一次打卡在 Day 18，之后无打卡，Day 21 结算 |
| **预期结果** | 结算取 Day 18 的体重作为最终体重（最后一次有效打卡体重）|
| **关联规则** | DR-002 / DR-006 |
| **状态** | ⏳ 待执行 |

---

### 3.8 M7 合规测试

#### TC-COMP-001｜首次使用弹出隐私同意框

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **预期结果** | 首次进入体重相关功能，弹出《敏感个人信息处理说明》单独同意框 |
| **状态** | ⏳ 待执行 |

#### TC-COMP-002｜拒绝同意后功能限制

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **预期结果** | 拒绝后不能录入体重/进行打卡，弹框提示引导 |
| **状态** | ⏳ 待执行 |

#### TC-COMP-003｜免责声明展示

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **预期结果** | 应用内可见「不替代医疗建议」类免责声明，文案无违禁词 |
| **状态** | ⏳ 待执行 |

#### TC-COMP-004｜数据删除入口可用

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **操作步骤** | 进入「我的」→ 找到「删除账号」→ 确认 |
| **预期结果** | 入口可见可用，删除后数据清除，不可恢复 |
| **状态** | ⏳ 待执行 |

#### TC-COMP-005｜排行榜接口不返回他人绝对体重

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **验证方式** | Charles 抓包 |
| **预期结果** | `GET /api/leaderboard` 不含他人 weight 字段 |
| **状态** | ⏳ 待执行 |

#### TC-COMP-006｜照片访问权限隔离

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **操作步骤** | 用 UserB 的 token 直接请求 UserA 的打卡照片 fileID |
| **预期结果** | 返回 403 或无权限错误，无法访问他人照片 |
| **状态** | ⏳ 待执行 |

#### TC-COMP-007｜体重字段不出现在服务端日志

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **验证方式** | 打卡后查看服务端日志（请求日志/错误日志/定时任务日志）|
| **预期结果** | 日志中不出现明文体重数值，只有脱敏标识或加密字符串 |
| **状态** | ⏳ 待执行（需韦韦配合提供日志访问）|

#### TC-COMP-008｜体重字段不出现在前端本地存储

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **验证方式** | 微信开发者工具「Storage」面板查看 |
| **预期结果** | wx.getStorageSync 中不含明文体重值 |
| **状态** | ⏳ 待执行 |

#### TC-COMP-009｜DB 体重字段加密存储

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **验证方式** | 直接查询 DB weight 字段（需斯斯提供测试环境只读权限）|
| **预期结果** | DB 中 weight 字段存储为密文，非明文数字；解密后值正确 |
| **状态** | ⏳ 待执行（约 Day 8 接口可用后）|

#### TC-COMP-010｜水印不含地理位置

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **预期结果** | 水印内容仅含时间+昵称，无 GPS/地址信息 |
| **状态** | ⏳ 待执行 |

#### TC-COMP-011｜文案违禁词扫描

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **检查项** | 全小程序文案不含「最」「绝对」「100%」「无效退款」「躺瘦」等广告法违禁词 |
| **状态** | ⏳ 待执行 |

---

### 3.9 退出与解散

#### TC-EXIT-001｜普通成员退出 PK

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **操作步骤** | 1. 非房主用户进入房间设置 2. 点击「退出PK」3. 确认 |
| **预期结果** | ① 状态标记「已退出」② 不参与后续排名和结算 ③ 历史打卡数据保留至 PK 结束 |
| **状态** | ⏳ 待执行 |

#### TC-EXIT-002｜退出后历史数据保留

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **预期结果** | 退出后历史打卡记录不物理删除，PK 结束后清理（按 DR-003 Q6）|
| **状态** | ⏳ 待执行 |

#### TC-EXIT-003｜退出后排行榜展示

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **预期结果** | 已退出用户排在末位，显示「已退出」标签，其他成员正常排名 |
| **状态** | ⏳ 待执行 |

#### TC-EXIT-004｜房主不可直接退出

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **操作步骤** | 房主点击「退出PK」|
| **预期结果** | 提示「房主不可退出，请选择解散房间」，退出操作被拦截 |
| **状态** | ⏳ 待执行 |

#### TC-EXIT-005｜解散后不触发结算

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **操作步骤** | 房主解散房间 |
| **预期结果** | ① 不生成结算快照 ② 不生成战报海报 ③ 状态为 DISSOLVED，非 SETTLED |
| **状态** | ⏳ 待执行 |

#### TC-EXIT-006｜解散后成员30天只读

| 字段 | 内容 |
|------|------|
| **优先级** | P2 |
| **预期结果** | 解散后 30 天内所有成员可查看历史打卡，但不能新增打卡/修改任何内容 |
| **状态** | ⏳ 待执行 |

#### TC-EXIT-007｜30天后数据清理

| 字段 | 内容 |
|------|------|
| **优先级** | P2 |
| **预期结果** | 解散 30 天后房间数据被清理，入口消失（可通过提前调整 DB 中解散时间验证）|
| **状态** | ⏳ 待执行 |

---

## 四、接口测试用例（基于接口约定 v0.1）

### 4.1 P0 接口（主链路）

| 接口 | 方法 | 路径 | 关键测试点 |
|------|------|------|-----------|
| 微信登录 | POST | `/api/auth/login` | code换token、新/老用户区分、token有效性 |
| 刷新token | POST | `/api/auth/refresh` | token刷新幂等、过期后403 |
| 创建PK | POST | `/api/room/create` | 参数校验（周期7-90天）、状态初始化ACTIVE |
| 加入PK | POST | `/api/room/join` | 邀请码校验、重复加入拦截2002、初始体重写入 |
| 体重打卡 | POST | `/api/checkin/weight` | 体重边界值、photoFileId必填、幂等性3001 |
| 饮食打卡 | POST | `/api/checkin/diet` | 文字长度校验、图片可选 |
| 运动打卡 | POST | `/api/checkin/exercise` | 文字长度校验、图片可选 |
| 排行榜 | GET | `/api/leaderboard` | 不含他人绝对体重、排序正确、并列处理(1,1,2) |
| 结算触发 | POST | `/api/settlement/trigger` | 自动触发/幂等/取最后有效体重 |
| 结算数据 | GET | `/api/settlement/{roomId}` | 数据一致性、绝对体重仅本人 |

### 4.2 P1 接口（异常路径覆盖）

| 接口 | 关键错误码 | 覆盖场景 |
|------|-----------|---------|
| `GET /api/room/{id}` | 2001 | 房间不存在 |
| `GET /api/room/list` | — | 空列表、多PK |
| `GET /api/checkin/today` | — | 当日打卡状态 |
| `PUT /api/user/profile` | 1001 | 昵称超长（>30字）|
| `DELETE /api/user/account` | — | 二次确认、数据清除验证 |
| `POST /api/room/dissolve` | 1003 | 非房主操作被拒 |

### 4.3 错误码全覆盖

| 错误码 | 含义 | 触发场景 |
|--------|------|---------|
| 1001 | 参数校验失败 | 体重超范围、周期非法、必填项缺失 |
| 1002 | 未登录/token失效 | 无token或过期 |
| 1003 | 无权限 | 操作非自己参与的PK |
| 2001 | 房间不存在 | 无效roomId |
| 2002 | 已在房间中 | 重复加入 |
| 2003 | PK已结束 | 结束后操作 |
| 3001 | 今日已打卡 | ⚠️ 取决于O-3/O-10确认结果 |
| 3002 | 打卡不在PK周期内 | 未开始/已结束 |
| 5000 | 系统内部错误 | 异常注入（DB断连等）|

---

## 五、待实现功能预留 List（L2/L3 Future）

> 以下为 **L1 未覆盖、未来迭代需测试的功能预留清单**，测试侧已备案，后续功能上线时直接对应测试。

### 5.1 功能增强类

| # | 功能 | 说明 | 预计版本 |
|---|------|------|---------|
| F-01 | 补卡机制 | L1 不做，L2 实现。每场最多补 2 次，需测试：补卡积分是否按实际天数计、补卡是否触发通知 | L2 |
| F-02 | 进度看板/体重曲线 | 体重趋势图、剩余天数 | L2 |
| F-03 | 打卡提醒（订阅消息） | 微信订阅消息推送，每日提醒，需真机测试 | L2 |
| F-04 | 发起人管理权 | 踢人、改房间名、提前结束、转让房主 | L2 |
| F-05 | 减重积分公式优化 | 引入3日均值或调整K值，减少体重波动影响 | L2 |
| F-06 | 服务端水印校验 | L1 仅前端Canvas水印，L2 补充服务端时间戳/哈希验证 | L2 |
| F-07 | 相册上传（非防作弊场景） | 饮食/运动打卡是否支持相册（O-11 Q16 待确认） | L2 |

### 5.2 商业化类

| # | 功能 | 说明 | 预计版本 |
|---|------|------|---------|
| F-08 | 押金/奖金机制 | 微信支付、退款、资金分配、对账 | L3 |
| F-09 | 陌生人匹配 | 公开 PK 房间、匹配机制 | L3 |
| F-10 | 会员/付费功能 | 订阅制或按次付费 | L3 |

### 5.3 社交互动类

| # | 功能 | 说明 | 预计版本 |
|---|------|------|---------|
| F-11 | 打卡点赞/评论 | 社区互动 | L2 |
| F-12 | 分享到朋友圈 | 朋友圈分享卡片样式 | L2 |
| F-13 | 排行榜照片缩略图 | O-6 待确认，若实现需测隐私隔离 | L2 |

### 5.4 AI/硬件扩展类

| # | 功能 | 说明 | 预计版本 |
|---|------|------|---------|
| F-14 | AI 食物识别 | 饮食拍照自动识别卡路里 | L3 |
| F-15 | 智能体重秤接入 | 蓝牙/WiFi 秤自动录入体重，需防篡改测试 | L3 |
| F-16 | 食物库 | 手动查询食物营养 | L3 |

### 5.5 合规增强类

| # | 功能 | 说明 | 预计版本 |
|---|------|------|---------|
| F-17 | PIPL 数据导出 | 用户申请导出自己所有数据 | L2 |
| F-18 | 内容审核 | 打卡照片 AI 审核（过滤违规内容）| L2 |
| F-19 | 健康风险提示 | 体重下降过快时弹出健康提示 | L2 |

---

## 六、⚠️ 阻塞项汇总（需产品确认后解锁）

| # | 问题 | 影响用例 | 待确认方 |
|---|------|---------|---------|
| O-2 | 增重时减重积分下限（0 vs 负分） | TC-SCORE-003 | 菲菲 |
| O-3 | 每日打卡次数（允许覆盖 vs 仅一次） | TC-CI-007, TC-CI-010, 错误码3001 | 菲菲+斯斯 |
| O-8 | 减重公式（单次体重 vs 3日均值） | TC-SCORE 全系列 | 菲菲 |
| O-9 | 积分更新时机（T+1 vs 实时）| TC-RANK-004, TC-SCORE-014 | 菲菲（建议T+1）|
| O-10 | 当日重复打卡规则 | TC-CI-007 | 菲菲 |
| O-11 | 积分分母统一口径 | TC-SCORE-005/012 | 菲菲（建议DR-006 Q8）|

> O-2/O-3/O-8 优先，影响 P0 用例预期值。预计菲菲确认后 **1 个工作日内**完成全部 P0 用例锁定。

---

## 七、准出 Checklist（Day 17 执行）

| 项目 | 标准 | 责任人 |
|------|------|--------|
| P0 缺陷 | 0 遗留 | CC |
| P1 缺陷 | 0 遗留 | CC |
| P2 缺陷 | ≤2 遗留且有规避方案 | CC+产品确认 |
| 冒烟回归 | 100% 通过 | CC |
| 积分计算复核 | Excel手算 vs 系统结果完全一致 | CC |
| 隐私合规 | 绝对体重不泄露（接口层+日志层+存储层均验证）| CC+斯斯 |
| 合规条款 | 同意框+免责声明+数据删除入口 均验证通过 | CC |
| 真机验证 | ≥2 台不同机型验证主链路 | CC |
| 安全组检查 | 3306/6379 只内网可访问 | 韦韦 |
| 域名/HTTPS | 小程序合法域名配置正确 | 韦韦 |

---

*文档位置：`05-测试用例报告/测试用例报告-v0.1.md`*
*关联策略：`05-测试用例报告/测试策略-v0.3.md`（同目录）*
*变更记录：v0.1 初稿（2026-07-14）*
